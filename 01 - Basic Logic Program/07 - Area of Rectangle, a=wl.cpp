#include<stdio.h>
main()
{
	int a,w,l;
	printf("Enter the value of w:");
	scanf("%d", &w);
	printf("Enter the value of l:");
	scanf("%d", &l);
	a=w*l;
	printf("The area of the rectengle (a) is %d", a);
	
	
}
